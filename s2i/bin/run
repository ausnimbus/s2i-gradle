#!/bin/bash -e

echo "---> Starting the application ..."

if [ -f "./gradlew" ]; then
    GRADLE_CMD="./gradlew"
else
    GRADLE_CMD="gradle"
fi

# run
$GRADLE_CMD -q run

ERR=$?
if [ $ERR -ne 0 ]; then
    echo "Aborting due to error code $ERR"
    exit $ERR
fi

